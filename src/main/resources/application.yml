
spring:
  datasource:
    password: letmein
    url: jdbc:postgresql://localhost:5432/serverdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    server:
      port: 8080